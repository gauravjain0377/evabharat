services:
  - type: web
    name: mediasync
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    region: oregon
    envVars:
      - key: PORT
        value: "8080"
      - key: DATABASE_PATH
        value: /app/data/media.db
      - key: STATIC_DIR
        value: /app/public
