# MediaSync Environment Configuration

# Server Port (default: 8080)
PORT=8080

# SQLite Database Location
DATABASE_PATH=./backend/data/media.db

# Optional static frontend directory for single-binary hosting
STATIC_DIR=./frontend/dist

# Frontend Vite configuration (when running Vite dev server)
VITE_API_URL=http://localhost:8080
